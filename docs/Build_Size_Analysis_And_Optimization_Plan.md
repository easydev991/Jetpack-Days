# План анализа и оптимизации размера AAB-файла

## Обзор проблемы

### Фактический размер AAB-файла (данные анализа APK Analyzer)

- **Base:** 13.7 MB
- **DEX (код):** 12.8 MB (93% от общего размера)
- **Ресурсы:** <2 MB (7%)

### Вывод из анализа

**93% размера - это код (DEX)**, а не ресурсы. Это **нормально** для приложения с Jetpack Compose + Room + Material3. AndroidX библиотеки занимают первое место по размеру.

### Что поможет оптимизировать

**Не поможет (так как отсутствуют/минимальны):**

- ❌ ABI splits - нет нативных библиотек .so
- ❌ Оптимизация изображений - занимают <7%
- ❌ Оптимизация локализаций - минимальное влияние

**Поможет:**

- ✅ R8/ProGuard оптимизация кода
- ✅ Агрессивные ProGuard правила
- ✅ Удаление неиспользуемых зависимостей

### Реалистичные цели

**Текущее состояние:** 13.7 MB - нормальный размер для приложения с Compose + Room

**Ожидаемый результат после R8/ProGuard:** ~10-12 MB (уменьшение на 10-20%)

**Идеальный результат:** ~8-10 MB (уменьшение на 25-40%)

---

## Этап 1. Анализ текущего состояния сборки ✅ (выполнено)

**Дата анализа:** 15.01.2026

**Результаты APK Analyzer:**

- Base: 13.7 MB
- DEX (код): 12.8 MB (93%) - AndroidX библиотеки занимают первое место
- Ресурсы: <2 MB (7%)
- Нативные библиотеки: отсутствуют (Compose использует чистый Kotlin)

**Вердикт:** Размер 13.7 MB - нормальный для современного Android приложения с Compose и Room. Основной размер - это неизбежные библиотеки AndroidX.

### 1.1. Анализ зависимостей

```bash
./gradlew app:dependencies --configuration releaseRuntimeClasspath
```

Изучить дерево зависимостей и выявить неиспользуемые библиотеки.

### 1.2. Анализ с bundletool (опционально)

```bash
brew install bundletool
bundletool build-apks --bundle=app-release.aab --output=apks.apks --mode=universal --ks=../android-secrets/jetpackdays/keystore/dayscounter-release.keystore --ks-key-alias=dayscounter
bundletool get-size total --apks=apks.apks
```

---

## Этап 2. Оптимизация build.gradle.kts

**Важно:** На основе анализа (DEX = 93%), актуальны только оптимизации кода. Оптимизации ресурсов, ABI splits и изображений **не дадут эффекта**.

### 2.1. Включение R8/ProGuard для Release сборки 🔥 (ПРИОРИТЕТ №1)

Проверить `app/build.gradle.kts`:

```kotlin
buildTypes {
    release {
        isMinifyEnabled = true
        isShrinkResources = true
        proguardFiles(
            getDefaultProguardFile("proguard-android-optimize.txt"),
            "proguard-rules.pro"
        )
        signingConfig = signingConfigs.getByName("release")
    }
}
```

### 2.2. Настройка ProGuard правил 🔥 (ПРИОРИТЕТ №2)

Проверить `app/proguard-rules.pro`:

```
# Compose
-keep class androidx.compose.** { *; }
-dontwarn androidx.compose.**

# Room
-keep class * extends androidx.room.RoomDatabase
-dontwarn androidx.room.paging.**

# Coroutines
-keepnames class kotlinx.coroutines.internal.MainDispatcherFactory {}
-keepnames class kotlinx.coroutines.CoroutineExceptionHandler {}

# Remove logging in release
-assumenosideeffects class android.util.Log {
    public static *** d(...);
    public static *** v(...);
    public static *** i(...);
}
```

### 2.3. Исключение Compose UI Tooling из Release 🟡 (опционально)

Проверить `build.gradle.kts`:

```kotlin
dependencies {
    implementation(platform(libs.androidx.compose.bom))

    // Debug implementation для ui-tooling
    debugImplementation("androidx.compose.ui:ui-tooling")
    debugImplementation("androidx.compose.ui:ui-tooling-preview")
}
```

### 2.4. Проверка зависимостей в `libs.versions.toml` 🟡 (опционально)

Проверить каждую зависимость:

- Все ли используются?
- Есть ли легковесные альтернативы?

**Ожидаемый эффект:** <0.5 MB (если найдутся неиспользуемые зависимости)

---

## Этап 3. Проверка после оптимизаций

### 3.1. Пересборка Release AAB

```bash
./gradlew clean assembleRelease
```

### 3.2. Повторный анализ в APK Analyzer

Сравнить с первоначальным анализом:

- Общий размер файла
- Размер каждой секции (DEX, res, lib)

### 3.3. Тестирование функциональности

Проверить критические сценарии:

1. Список событий отображается
2. Создание/редактирование событий работает
3. Сохранение в Room базу
4. Резервное копирование/восстановление JSON
5. Навигация между экранами
6. Обработка ошибок

### 3.4. Генерация отчета bundletool (опционально)

```bash
bundletool get-size total --apks=apks.apks --bundles=app-release.aab
```

---

## Этап 4. Документация результатов

### 4.1. Создание отчета

Создать файл `docs/Build_Size_Analysis_Report.md`:

```markdown
# Отчет анализа размера сборки

## Исходное состояние
- Размер AAB: 13.7 MB
- Дата анализа: 15.01.2026
- DEX: 12.8 MB (93%)
- Ресурсы: <2 MB (7%)
- Нативные библиотеки: отсутствуют

## Примененные оптимизации
1. Включен R8/ProGuard
2. Настроены ProGuard правила
3. Исключен ui-tooling из release (если применимо)
4. Проверены зависимости

## Состояние после оптимизаций
- Размер AAB: X.X MB
- Уменьшение: X.X MB (XX%)
- DEX: X.X MB
- Ресурсы: X.X MB
```

### 4.2. Обновление документации проекта

Обновить `docs/App_Publication_Plan.md` с информацией о:

- Текущем размере сборки (13.7 MB до оптимизаций)
- Примененных оптимизациях
- Рекомендациях для поддержки размера в будущем

---

## Критерии успеха

### Фактическое состояние (до оптимизаций)

- Размер AAB: **13.7 MB**
- DEX (код): **12.8 MB (93%)**
- Ресурсы: **<2 MB (7%)**

### Реалистичные цели

**Минимальный успех (R8/ProGuard):**

- Размер AAB: <12.5 MB (уменьшение на 5-10%)
- Причина: R8 удалит часть неиспользуемого кода из AndroidX библиотек

**Результативная оптимизация (R8 + ProGuard правила):**

- Размер AAB: <11 MB (уменьшение на 15-20%)
- Причина: Агрессивные ProGuard правила удалят больше неиспользуемого кода

**Идеальный результат (максимальная оптимизация):**

- Размер AAB: <10 MB (уменьшение на 25-30%)
- Причина: Максимальное удаление неиспользуемого кода и зависимостей

### Общий вердикт

Размер **13.7 MB** - это **нормальный и ожидаемый** размер для современного Android приложения с:

- Jetpack Compose (~8-10 MB кода)
- Room Database (~2-3 MB кода)
- Material3 (~1-2 MB кода)

Сравнение с другими приложениями:

- Приложения с XML Views: ~5-8 MB
- Приложения с Compose: ~10-15 MB ✅ (наши данные попадают в этот диапазон)
- Приложения с Compose + Room + Navigation: ~12-18 MB

---

## Порядок выполнения

1. **Анализ текущего состояния** ✅ (выполнено)
   - [x] Собран release AAB
   - [x] Проанализирован в APK Analyzer
   - [x] Выявлено: DEX = 93%, ресурсы = 7%

2. **Оптимизация конфигурации**
   - [ ] Включить R8/ProGuard 🔥 (ПРИОРИТЕТ №1)
   - [ ] Настроить ProGuard правила 🔥 (ПРИОРИТЕТ №2)
   - [ ] Проверить и оптимизировать зависимости 🟡 (опционально)
   - [ ] Исключить ui-tooling из release 🟡 (опционально)

3. **Тестирование**
   - [ ] Пересобрать release AAB
   - [ ] Проанализировать новый размер в APK Analyzer
   - [ ] Протестировать функциональность приложения

4. **Документация**
   - [ ] Создать отчет о результатах
   - [ ] Обновить документацию проекта

---

## Риски и mitigations

| Риск | Вероятность | Влияние | Mitigation |
|------|-------------|---------|------------|
| R8 удалит важный код через reflection | Средняя | Высокое | Добавить правила в proguard-rules.pro, протестировать |
| Room database не работает после ProGuard | Низкая | Высокое | Добавить ProGuard правила для Room |
| Compose Preview сломается | Низкая | Низкое | Использовать debugImplementation для ui-tooling |
| Функциональность нарушена после оптимизации | Средняя | Высокое | Полное тестирование всех сценариев |

---

## Полезные команды

```bash
# Очистка и сборка release
./gradlew clean assembleRelease

# Анализ зависимостей
./gradlew app:dependencies --configuration releaseRuntimeClasspath

# Lint анализ
./gradlew lintRelease

# Генерация отчета о зависимостях
./gradlew app:dependencyInsight --dependency room-runtime
```

## Полезные ссылки

- [Android App Bundle Size](https://developer.android.com/topic/performance/app-size)
- [Reduce APK size](https://developer.android.com/topic/performance/reduce-apk-size)
- [Compose Performance](https://developer.android.com/jetpack/compose/performance)
- [R8/Shrink](https://developer.android.com/build/shrink-code)
- [Build > Analyze APK](https://developer.android.com/studio/build/apk-analyzer)
