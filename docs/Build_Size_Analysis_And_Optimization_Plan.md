# План анализа и оптимизации размера AAB-файла

## Обзор проблемы

**История сборок:**

| № | Размер AAB | Коммит | Описание изменений |
|---|------------|--------|------------------|
| 1 | 13.7 MB | Начальный анализ | Без R8/ProGuard |
| 2 | 5.6 MB | `7d34a82` | Интеграция Firebase + R8/ProGuard |
| 3 | 5.6 MB | После оптимизации | Стабильный размер |
| 4 | 6.5 MB | `fd22844` (аналитика), `97f56a4` (сборка) | Firebase Analytics (+0.9 MB) |

### Результаты оптимизации (15-16.01.2026)

**До оптимизации (сборка №1):**

- Размер: 13.7 MB
- DEX: 12.8 MB (93% от общего размера)
- Ресурсы: <2 MB (7%)

**После R8/ProGuard (сборка №2):**

- Размер: 5.6 MB
- Уменьшение: 59% (8.1 MB)
- DEX: 1.6 MB (75% уменьшение)

**Текущий размер (сборка №4):**

- Размер: 6.5 MB
- Увеличение: +0.9 MB (+16%) относительно сборки №3
- Причина: Firebase Analytics (коммиты `fd22844` - код аналитики, `97f56a4` - сборка)

### Что изменилось с DEX

**До R8/ProGuard:**

| Метрика | Было | Стало |
|----------|-------|-------|
| `classes` | 2.1 MB | - |
| `classes2` | 2.0 MB | - |
| `classes3` | 2.2 MB | - |
| `classes` (объединён) | - | 1.6 MB |
| **Итого DEX** | 6.3 MB | **1.6 MB** (75% уменьшение) |

**Причина уменьшения:** Включение `isMinifyEnabled = true` в коммите `7d34a82` (Интеграция Firebase)

**Причина увеличения (сборка №3 → №4):**

- Добавлен Firebase Analytics
- Добавлена новая зависимость в `libs.versions.toml`
- Добавлен код аналитики:
  - `FirebaseAnalyticsHelper.kt` (+74 строки)
  - `NavigationAnalyticsExtensions.kt` (+55 строк)
  - `RootScreenComponents.kt` (+51 строк)
- Общий рост: +187 строк кода + новая библиотека

---

## Анализ увеличения размера (сборка №3 → №4)

### Что изменилось

**Описание:** Аналитика для разбора крашей

**Изменения в коде:**

- `app/build.gradle.kts` (+3 строки) - добавлена зависимость Firebase Analytics
- `app/src/main/AndroidManifest.xml` (+3 строки) - добавлена конфигурация
- `FirebaseAnalyticsHelper.kt` (+74 строки) - helper для аналитики
- `NavigationAnalyticsExtensions.kt` (+55 строк) - расширения для навигации
- `RootScreenComponents.kt` (+51 строка) - компоненты экрана
- `gradle/libs.versions.toml` (+1 строка) - новая зависимость

**Общий рост:** +187 строк кода + новая библиотека

### Почему увеличился размер

1. **Новая зависимость:** Firebase Analytics (firebase-analytics)
2. **Новый код:** +187 строк Kotlin кода
3. **R8 оптимизация:** R8 не смог удалить часть кода аналитики (нужна для production)

### Ожидаемый размер

Расчет размера:

- Базовый размер (сборка №3): 5.6 MB
- Firebase Analytics: ~0.5-0.8 MB (типичный размер)
- Код аналитики: ~0.1-0.3 MB (с R8 оптимизацией)
- Итого ожидаемый: ~6.2-6.7 MB

**Фактический размер:** 6.5 MB ✅ (попадает в ожидаемый диапазон)

### Вердикт

Увеличение размера на +0.9 MB (+16%) - **нормально** для:

- Firebase Analytics (~0.7 MB)
- 187 строк нового кода

Размер 6.5 MB всё ещё **отличный** для приложения с:

- Jetpack Compose
- Room Database
- Navigation
- Firebase Crashlytics
- Firebase Analytics

**Сравнение:**

- Типичное приложение с Compose + Firebase: ~15-20 MB
- Наше приложение: 6.5 MB ✅ (в 2-3 раза меньше!)

---

## Рекомендации по оптимизации (будущее)

### Текущий размер: 6.5 MB

Если нужно уменьшить размер в будущем:

1. **Проверить неиспользуемые зависимости** (эффект: <0.5 MB)
2. **Удалить неиспользуемый код** (эффект: <0.3 MB)
3. **Агрессивные ProGuard правила** (эффект: ~0.5-1 MB, но риски)

### Приоритеты

- 🔥 **Не оптимизировать Firebase Analytics** - важна для production-мониторинга
- 🔥 **Сохранить R8/ProGuard** - даёт максимальный эффект (75% уменьшение DEX)
- 🟡 **Проверить зависимости** - только если есть подозрение на неиспользуемые
- ⚪ **Не использовать ABI splits** - нет нативных библиотек .so

---

## Этап 1. Анализ текущего состояния сборки ✅

**Вердикт:** 93% размера - это код (DEX), 7% - ресурсы. **Нормально** для Compose + Room + Material3.

**Актуально:**

- ✅ R8/ProGuard оптимизация кода
- ✅ Удаление неиспользуемых зависимостей
- ❌ ABI splits (нет нативных библиотек)
- ❌ Оптимизация изображений (<7% размера)

---

## Этап 2. Оптимизация build.gradle.kts ✅

### 2.1-2.3. Включение R8/ProGuard ✅

**Выполнено:**

- ✅ `isMinifyEnabled = true` (коммит `7d34a82`)
- ✅ ProGuard правила для Crashlytics настроены
- ✅ UI Tooling исключён через `debugImplementation`

**Результат:** DEX уменьшен с 6.3 MB до 1.6 MB

### 2.4. Проверка зависимостей ⏳ (опционально)

Проверить каждую зависимость на использование:

```bash
./gradlew app:dependencies --configuration releaseRuntimeClasspath
```

**Ожидаемый эффект:** <0.5 MB (если найдутся неиспользуемые)

---

## Этап 3. Проверка после оптимизаций

### 3.1-3.2. Сборка и анализ ✅

**Результаты (сборка №2):**

- Размер AAB: 5.6 MB (уменьшение на 59%)
- DEX: 1.6 MB (уменьшение на 75%)
- Ресурсы: <2 MB (сохранены)

**Текущий размер (сборка №4):**

- Размер AAB: 6.5 MB
- Увеличение: +0.9 MB (+16%) относительно сборки №3
- Причина: Firebase Analytics (+187 строк кода + новая зависимость)

### 3.3. Тестирование функциональности ⏳ (критично перед публикацией)

Проверить:

- Список событий отображается
- Создание/редактирование работает
- Сохранение в Room
- Резервное копирование/восстановление JSON
- Навигация
- Crashlytics в release
- Firebase Analytics в release

### 3.4. Проверка крашей в production 📊 (после публикации)

1. Firebase Console → Crashlytics
2. Firebase Console → Analytics
3. Проверить декодирование стек-трейсов
4. Добавить ProGuard правила (при необходимости)

---

## Этап 4. Документация результатов

### 4.1. Создание отчета ⏳

Создать `docs/Build_Size_Analysis_Report.md` с данными о до/после оптимизации.

### 4.2. Обновление документации проекта ⏳

Обновить `docs/App_Publication_Plan.md`:

- Текущий размер: 6.5 MB (сборка №4)
- Примененные оптимизации
- Рекомендации

---

## Критерии успеха

### До оптимизаций

- Размер: 13.7 MB
- DEX: 12.8 MB (93%)

### Реалистичные цели vs Факт

| Цель | Ожидание | Факт (сборка №2) | Факт (сборка №4) | Статус |
|-------|-----------|--------------------|-------------------|---------|
| Минимальный успех | <12.5 MB (5-10%) | 5.6 MB (59%) | 6.5 MB (53%) | ✅ Превышен |
| Результативная оптимизация | <11 MB (15-20%) | 5.6 MB (59%) | 6.5 MB (53%) | ✅ Превышен |
| Идеальный результат | <10 MB (25-30%) | 5.6 MB (59%) | 6.5 MB (53%) | ✅ Превышен |

**Вердикт:** Отличный результат! 6.5 MB - отличный размер для Compose + Room + Navigation + Firebase Analytics.

Сравнение с другими приложениями:

- XML Views: ~5-8 MB
- Compose: ~10-15 MB
- Compose + Room + Nav: ~12-18 MB
- Compose + Room + Nav + Firebase: ~15-20 MB
- **Наше приложение:** ~6.5 MB ✅ (с Firebase Analytics!)

---

## Порядок выполнения

1. **Анализ** ✅
2. **Оптимизация** ✅
   - [x] R8/ProGuard (коммит `7d34a82`)
   - [x] ProGuard правила
   - [x] UI Tooling исключён
   - [x] Firebase Analytics (коммиты `fd22844` - код, `97f56a4` - сборка)
   - [ ] Проверка зависимостей 🟡
3. **Тестирование** ⏳
   - [x] Сборка и анализ ✅
   - [ ] Функциональность (критично)
   - [ ] Краши в production
4. **Документация** ⏳
   - [ ] Создать отчет
   - [ ] Обновить документацию

---

## Риски и mitigations

| Риск | Mitigation |
|------|------------|
| Функциональность нарушена | ⏳ Полное тестирование (критично) |
| Краши из-за оптимизации | 📊 Проверить Firebase Console после публикации |

---

## Что делать перед публикацией

### Критические шаги

1. **Полное тестирование функциональности**
2. **Мониторинг через Firebase Crashlytics** (1-2 дня после публикации)

---

## Тестирование AAB в Android Studio

### Способ 1: Установка через ADB (рекомендуется)

**Преимущества:**

- Быстрая установка на устройство/emulator
- Можно сразу тестировать функциональность
- Не требует пересборки проекта

**Инструкция:**

1. **Установить bundletool** (если ещё не установлен):

```bash
brew install bundletool
```

2. **Развернуть AAB в универсальный APK:**

```bash
# В директории с AAB файлом (используйте актуальное имя файла, например dayscounter10.aab)
bundletool build-apks \
  --bundle=dayscounter10.aab \
  --output=dayscounter.apks \
  --mode=universal \
  --ks=.secrets/keystore/dayscounter-release.keystore \
  --ks-key-alias=upload \
  --ks-pass=YOUR_KEYSTORE_PASSWORD \
  --ks-key-pass=YOUR_KEY_PASSWORD
```

Примечание: Используйте актуальное имя файла сборки (например, `dayscounter10.aab` для сборки № 10)

3. **Извлечь универсальный APK:**

```bash
# APK будет в dayscounter.apks/universal.apks/
unzip dayscounter.apks -d apks-unzipped
```

4. **Установить на устройство/emulator:**

```bash
# Проверить подключенные устройства
adb devices

# Установить APK
adb install -r apks-unzipped/universal.apk
```

5. **Запустить приложение:**

```bash
adb shell am start -n com.dayscounter/.MainActivity
```

---

### Способ 2: Через Android Studio (если устройство подключено)

**Инструкция:**

1. **Подключить Android устройство/emulator**

2. **Открыть проект в Android Studio**

3. **Создать Run Configuration для release:**

   - Run > Edit Configurations...
   - Добавить Android App
   - Module: `app`
   - Build Variant: `release`
   - Deploy: `Default APK`

4. **Установить keystore** (если ещё не настроен):

   - File > Project Structure > Signing Configs
   - Создать release конфигурацию:
     - Keystore: `.secrets/keystore/dayscounter-release.keystore`
     - Key alias: `upload`
     - Passwords: из секретов

5. **Запустить приложение:**

   - Shift + F10 (Run 'app')
   - Приложение установится на устройство/emulator

---

### Способ 3: Через Google Play Internal Testing (для реального тестирования)

**Инструкция:**

1. **Создать релиз в Google Play Console:**
   - App bundle: загруженный AAB
   - Release type: Internal Testing

2. **Создать список тестировщиков:**

3. **Опубликовать и поделиться ссылкой:**

4. **Тестировщики устанавливают через Google Play Store**

**Преимущества:**

- Реальный процесс установки
- Тестирование на разных устройствах
- Прогрев Google Play Store

---

### Рекомендации по тестированию

**Критические сценарии:**

- [ ] Список событий отображается корректно
- [ ] Создание события работает
- [ ] Редактирование события работает
- [ ] Удаление события работает
- [ ] Сохранение в Room базу
- [ ] Резервное копирование в JSON
- [ ] Восстановление из JSON
- [ ] Навигация между экранами
- [ ] Обработка ошибок (проверить Crashlytics в release)
- [ ] Тёмная/светлая тема переключается
- [ ] Смена языка работает

**Дополнительные проверки:**

- Проверить поведение при повороте экрана
- Проверить поведение при восстановлении из фона
- Проверить поведение при нехватке памяти
- Проверить поведение при плохом интернет-соединении (не должно влиять на офлайн-режим)

### Опциональные улучшения

- Проверить зависимости на неиспользуемые
- Агрессивные ProGuard правила (при необходимости)
