# Экран 1.1: Root Screen (Корневой экран с TabBar)

**Статус: Полностью реализован** ✅

## Обзор

Root Screen является основным экраном приложения с нижней навигацией. Он обеспечивает переключение между основными разделами приложения через TabBar.

## Назначение

Основной экран приложения с нижней навигацией, обеспечивающий переключение между вкладками "События" и "Ещё".

## Компоненты

- **Bottom Navigation Bar** — нижняя навигация с двумя вкладками ✅
- **Контейнер для содержимого** — отображение содержимого выбранной вкладки ✅
- **Управление состоянием** — управление активной вкладкой через ViewModel ✅

## Вкладки

- **Вкладка "События"** — главный список записей (Main Screen) ✅
- **Вкладка "Ещё"** — дополнительные функции и настройки (More Screen) ✅

## Зависимости от других этапов

- Нет зависимостей — это базовый экран навигации

---

## Реализация

Этап полностью завершен ✅

### Реализованные компоненты

- **`navigation/Screen.kt`** - sealed class с маршрутами (Events, More, ItemDetail, CreateItem, EditItem)
- **`viewmodel/RootScreenViewModel.kt`** - ViewModel для управления вкладками
- **`ui/state/RootScreenState.kt`** - UI State
- **`ui/screen/RootScreen.kt`** - RootScreen composable с NavigationBar и NavHost
- **`ui/screen/components/RootScreenComponents.kt`** - компоненты навигации и экраны

### Тесты

Все тесты реализованы и активны:

- **`RootScreenViewModelTest.kt`** - unit-тесты ViewModel (4 теста) с MockK
- **`RootScreenStateTest.kt`** - тесты UI State
- **`ScreenTest.kt`** - тесты sealed class Screen (2 теста)

**Важно:** Подробные правила тестирования см. в `.cursor/rules/testing.mdc`

---

## Блокируемые этапы

После завершения этого экрана можно приступать к:

- ✅ Экран 2.1: Главный экран (реализован)
- ✅ Экран 3.1: Item Screen / Детальный экран (реализован)
- ✅ Экран 4.1: Create/Edit Item Screen (реализован)
- 🔄 Экран 5.1: More Screen (частично реализован - заглушка)

---

## Примечания

- **Material Design 3**: Современные компоненты для навигации
- **Состояние**: Синхронизация вкладок с навигационным стеком через ViewModel
- **Тестирование**: Все тесты активны и покрывают все компоненты
- **Тема**: Поддержка светлого и темного режимов
- **Локализация**: Использованы ресурсы из `Localization_Plan.md` (`events`, `more`) через `titleResId`
- **Доп. маршруты**: `ItemDetail`, `CreateItem`, `EditItem` с параметрами `itemId`
- **Видимость навигации**: Скрыта на вторичных экранах, отображается только на главных вкладках

---

## Текущее состояние реализации

### Выполнено

- ✅ Навигация: sealed class Screen (Events, More) + дополнительные маршруты (ItemDetail, CreateItem, EditItem)
- ✅ Компоненты: RootScreen с NavigationBar, NavHost, все компоненты навигации
- ✅ Логика: RootScreenViewModel с управлением вкладками, синхронизация с навигационным стеком
- ✅ Интеграция: Main Screen (Events), Detail Screen, CreateEdit Screen
- ✅ Тесты: все тесты активны (RootScreenViewModel, Screen, интеграционные)
- ✅ UI: тема приложения, иконки, визуальное выделение вкладок, скрытие навигации на вторичных экранах

### Не выполнено

- ⚠️ **More Screen**: реализована только заглушка с текстом "Экран настроек не реализован"

---

## Файловая структура

```text
ui/screen/
├── RootScreen.kt                     # Главный экран с TabBar
└── components/
    └── RootScreenComponents.kt        # Компоненты навигации
        ├── navigationBarContent()     # Навигационная панель
        ├── navHostContent()           # Маршрутизация
        ├── eventsScreenContent()      # Экран событий
        └── moreScreenContent()        # Экран настроек (заглушка)

navigation/
└── Screen.kt                         # Sealed class для навигации (включая Events, More, ItemDetail, CreateItem, EditItem)

viewmodel/
├── RootScreenViewModel.kt             # ViewModel для управления вкладками
└── ui/state/
    └── RootScreenState.kt            # UI State

test/
└── ...
    ├── RootScreenViewModelTest.kt     # Тесты ViewModel (4 теста)
    ├── RootScreenViewModelIntegrationTest.kt  # Интеграционные тесты
    └── navigation/
        └── ScreenTest.kt             # Тесты sealed class Screen (2 теста)
```

---

## История изменений

- 2025-01-01: Базовая реализация (NavigationBar, NavHost, ViewModel, тесты, интеграция с Main Screen)
- 2026-01-01: Добавление маршрутов (ItemDetail, CreateItem, EditItem), интеграция с Detail Screen и CreateEdit Screen
- 2026-01-02: Обновление документации
